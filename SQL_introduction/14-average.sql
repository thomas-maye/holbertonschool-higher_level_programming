-- Create a script that computes the average score of all records in the table second_table of the database hbtn_0c_0
-- The resulat column name should be average
SELECT AVG(score) AS average FROM second_table;